{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Loom main-window capability: Ollama HTTP + session file I/O",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "fs:default",
    "window-state:default"
  ]
}
